# Accordion Expand

Accordion panel expands and collapses smoothly. Uses max-height transition with overflow hidden.

## Features

- Smooth expand/collapse animation
- Chevron rotation indicator
- Customizable easing and duration
- Pure CSS transitions

## Usage

Toggle `.aex-open` class on `.aex-item`. JS handles click toggling. CSS handles transitions.
